#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Interactive commit message generation is OPTIONAL, not mandatory
# Only use commitizen if explicitly requested with --commit
if [ "$2" = "--commit" ]; then
  exec < /dev/tty && npx cz --hook || true
fi 